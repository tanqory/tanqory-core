module.exports = {
  // Use single quotes for strings
  singleQuote: true,

  // Use trailing commas where valid in ES5 (objects, arrays, etc.)
  trailingComma: 'es5',

  // Print width - try to wrap lines that exceed this length
  printWidth: 100,

  // Use 2 spaces for indentation
  tabWidth: 2,

  // Use spaces instead of tabs
  useTabs: false,

  // Include semicolons at the ends of statements
  semi: true,

  // Only add quotes around object properties when required
  quoteProps: 'as-needed',

  // Use single quotes in JSX
  jsxSingleQuote: true,

  // Put the closing bracket on the same line as the last prop
  bracketSameLine: false,

  // Include spaces inside object literals
  bracketSpacing: true,

  // Always include parens around arrow function parameters
  arrowParens: 'always',

  // Format the entire range of the file
  rangeStart: 0,
  rangeEnd: Infinity,

  // Don't require pragma to format
  requirePragma: false,

  // Don't add pragma to the top of files
  insertPragma: false,

  // Use default line endings
  endOfLine: 'lf',

  // Format embedded code
  embeddedLanguageFormatting: 'auto',

  // HTML whitespace sensitivity
  htmlWhitespaceSensitivity: 'css',

  // Vue files script and style tags indentation
  vueIndentScriptAndStyle: false,
};
